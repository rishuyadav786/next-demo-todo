version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm ci
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: .next
    files:
      - 'standalone/**/*'
      - 'public/**/*'
      - 'static/**/*'
  cache:
    paths:
      - node_modules/**/*